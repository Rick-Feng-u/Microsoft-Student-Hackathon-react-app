import { PressableProps } from 'react-native';
import { PressabilityConfig, PressabilityEventHandlers, PressableFocusProps, PressableHoverProps } from './Pressability/Pressability.types';
export declare type IPressState = {
    pressed?: boolean;
};
export declare type IFocusState = {
    focused?: boolean;
};
export declare type IHoverState = {
    hovered?: boolean;
};
export declare type IPressableState = IPressState & IFocusState & IHoverState;
export declare type IPressableOptions = PressabilityConfig & {
    onStateChange?: (state: IPressableState) => void;
};
export declare type IWithPressableOptions<T extends object> = T & IPressableOptions;
export declare type IWithPressableEvents<T extends object> = T & PressabilityEventHandlers;
export declare type IPressableHooks<T extends object> = {
    props: IWithPressableEvents<T>;
    state: IPressableState;
};
/**
 * The hover props on RN 0.63 don't exist in react-native, only on the desktop SKUs, but we need them for hooking
 * mouse events on windows and macos. This adds them to the props type, stripping them from the base pressable type as
 * future proofing for when we upgrade to 0.64+.
 *
 * Similarly the focus methods exist at the View level but are not exposed on pressable props in 0.63. As a result they
 * need a similar treatment
 */
export declare type PressablePropsExtended = Exclude<PressableProps, 'onHoverIn' | 'onHoverOut' | 'onFocus' | 'onBlur'> & PressableHoverProps & PressableFocusProps;
//# sourceMappingURL=useAsPressable.types.d.ts.map