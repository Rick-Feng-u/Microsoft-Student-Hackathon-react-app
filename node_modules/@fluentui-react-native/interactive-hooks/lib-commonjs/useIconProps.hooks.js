"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var react_native_1 = require("react-native");
// this hook creates icon props from given source
function createIconProps(src) {
    if (src === undefined)
        return null;
    if (typeof src === 'number') {
        var rasterProps = { src: src };
        var asset = react_native_1.Image.resolveAssetSource(+src);
        return {
            rasterImageSource: rasterProps,
            width: asset.width,
            height: asset.height,
        };
    }
    else if (typeof src === 'string') {
        var rasterProps = { src: { uri: src } };
        return { rasterImageSource: rasterProps };
    }
    else {
        return src;
    }
}
exports.createIconProps = createIconProps;
//# sourceMappingURL=useIconProps.hooks.js.map