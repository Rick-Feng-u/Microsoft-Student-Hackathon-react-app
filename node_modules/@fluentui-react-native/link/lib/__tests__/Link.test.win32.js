import * as React from 'react';
import { View } from 'react-native';
import { Link } from '..';
import * as renderer from 'react-test-renderer';
it('Link all props', function () {
    var tree = renderer.create(React.createElement(Link, { content: "Link all props", url: "https://www.bing.com" })).toJSON();
    expect(tree).toMatchSnapshot();
});
it('Link with child', function () {
    var tree = renderer
        .create(React.createElement(Link, { content: "Link with child", url: "https://www.bing.com" },
        React.createElement(View, null)))
        .toJSON();
    expect(tree).toMatchSnapshot();
});
//# sourceMappingURL=Link.test.win32.js.map