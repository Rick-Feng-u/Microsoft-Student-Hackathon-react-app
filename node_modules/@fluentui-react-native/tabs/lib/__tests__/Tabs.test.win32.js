import * as React from 'react';
import { Tabs, TabsItem } from '..';
import * as renderer from 'react-test-renderer';
it('Tabs default props', function () {
    var tree = renderer
        .create(React.createElement(Tabs, null,
        React.createElement(TabsItem, { itemKey: "1" }),
        React.createElement(TabsItem, { itemKey: "2" }),
        React.createElement(TabsItem, { itemKey: "3" })))
        .toJSON();
    expect(tree).toMatchSnapshot();
});
it('Tabs disabled', function () {
    var tree = renderer
        .create(React.createElement(Tabs, null,
        React.createElement(TabsItem, { itemKey: "1", disabled: true }),
        React.createElement(TabsItem, { itemKey: "2", disabled: true }),
        React.createElement(TabsItem, { itemKey: "3" })))
        .toJSON();
    expect(tree).toMatchSnapshot();
});
it('Tabs header text and count', function () {
    var tree = renderer
        .create(React.createElement(Tabs, null,
        React.createElement(TabsItem, { headerText: "TabsItem", itemCount: 1, itemKey: "1" }),
        React.createElement(TabsItem, { headerText: "TabsItem", itemCount: 0, itemKey: "2" }),
        React.createElement(TabsItem, { headerText: "TabsItem", itemCount: 100, itemKey: "3" })))
        .toJSON();
    expect(tree).toMatchSnapshot();
});
it('Tabs headers only', function () {
    var tree = renderer
        .create(React.createElement(Tabs, { headersOnly: true },
        React.createElement(TabsItem, { itemKey: "1" }),
        React.createElement(TabsItem, { itemKey: "2" }),
        React.createElement(TabsItem, { itemKey: "3" })))
        .toJSON();
    expect(tree).toMatchSnapshot();
});
it('Tabs FocusZone props', function () {
    var tree = renderer
        .create(React.createElement(Tabs, { defaultSelectedKey: "2", isCircularNavigation: false },
        React.createElement(TabsItem, { itemKey: "1" }),
        React.createElement(TabsItem, { itemKey: "2" }),
        React.createElement(TabsItem, { itemKey: "3" })))
        .toJSON();
    expect(tree).toMatchSnapshot();
});
it('Tabs props', function () {
    var tree = renderer
        .create(React.createElement(Tabs, { label: "Tabs", defaultSelectedKey: "2", isCircularNavigation: false, headersOnly: true },
        React.createElement(TabsItem, { headerText: "TabsItem", itemCount: 1, itemKey: "1", disabled: true }),
        React.createElement(TabsItem, { headerText: "TabsItem", itemCount: 0, itemKey: "2" }),
        React.createElement(TabsItem, { headerText: "TabsItem", itemCount: 100, itemKey: "3" })))
        .toJSON();
    expect(tree).toMatchSnapshot();
});
//# sourceMappingURL=Tabs.test.win32.js.map