import { getAliasTokens } from '@fluentui-react-native/theme-tokens';
import { memoize } from '@fluentui-react-native/memo-cache';
import { mapPipelineToTheme } from './mapPipelineToTheme';
function createAliasTokensWorker(mode) {
    var aliasTokens = getAliasTokens(mode);
    return mapPipelineToTheme(aliasTokens);
}
export var createAliasTokens = memoize(createAliasTokensWorker);
//# sourceMappingURL=createAliasTokens.js.map