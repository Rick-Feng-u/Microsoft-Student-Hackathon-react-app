import { Appearance } from 'react-native';
import { AppTheme } from 'react-native-windows';
export function getCurrentAppearance(appearance, fallback) {
    if (appearance === undefined) {
        return fallback;
    }
    var isDynamic = appearance === 'dynamic';
    if (isDynamic) {
        if (!AppTheme || !AppTheme.isAvailable || !Appearance) {
            return fallback;
        }
        if (AppTheme.isHighContrast) {
            return 'highContrast';
        }
        else {
            return Appearance.getColorScheme(); // light or dark
        }
    }
    else {
        return appearance;
    }
}
//# sourceMappingURL=getCurrentAppearance.windows.js.map